/*realize uma sequencia logica de passsos (algoritmo) para receber o nome do aluno, 
duas notas e calcular a media deste aluno, dizendo se ele esta:
 aprovado (Media > = 6,0)
 reprovado(Meida < 4,0)
 exame (Media < = 6,0 >=4,0)

entre com o nome do aluno 
 
Escreva ("Digite o nome do auluno");
Leia(Nome_aluno);  // Digitando a variavel, nomes variam!

Escreva ("digite a primeira nota"); mensagem que aparece para o consumidor
Leia (N1); // notas variam, variavel 

Escreva("digite a segunda nota");
Leia (N2); // notas variam, variavel    

Media = (N1 + N2) / 2;

escreva ("O aluno e:", Nome_aluno); // conteudo da variavel 'alunos'

se (Media >= 6,0) entao // verifica uma condicao
    escreva("Aluno Reprovado com a Media:" , Media); verifica uma condicao 

se (Media < 4,0 ) entao // verifica uma condicao
    escreva ("Aluno Reprovado com a Media:" , Media);

se (media > = 4,0 e media < 6,0 ) entao //verifica uma condicao
    escreva ("Aluno em Exame com a Media:" , Media ); 

            FIM DO ALGORITMO!!
*/ 


